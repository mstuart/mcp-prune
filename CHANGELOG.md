# Changelog

## Unreleased

- Verify SHA-256 checksums for native release assets before npm postinstall unpacks them.
- Publish the npm wrapper with provenance via GitHub Actions trusted publishing.
- Add npm installer, shim, package, and `--version` smoke tests.
- Add CI coverage for Rust supply-chain advisories.
